# Auto detect text files and perform LF normalization
* text=auto

# Flashable firmware images must never be line-ending normalized or text-diffed.
*.bin binary

# Qualification evidence is hashed byte-for-byte by protected promotion.
tests/evidence/modern-v0.16.1-qualification.json -text
